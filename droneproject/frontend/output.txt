end_idx 92033
Printing Page 2 structure:
{/* PAGE 2: Pipeline & Results */}
                    <div className="w-[100vw] flex-shrink-0 flex items-center justify-center px-6 lg:px-12 relative">
                        <div className="w-full max-w-4xl flex flex-col gap-8">
                            <div className="text-center mb-2">
                                <h2 className="text-4xl font-black text-stone-800 tracking-tight mb-2">Real-Time Processing</h2>
                                <p className="text-stone-500 uppercase tracking-widest text-xs font-bold">Step 02 / Pipeline</p>
                            </div>

                            <div className="grid lg:grid-cols-[1.2fr_0.8fr] gap-8 w-full bg-white/70 rounded-[2.5rem] border border-stone-200/80 backdrop-blur-xl p-8 shadow-[0_20px_60px_rgba(0,0,0,0.05)]">
                                <div className="flex flex-col gap-4">
                                    <div className={`pipeline-scene ${sceneState}`}>
                                        <NeuralTreeScene
                                            sceneState={sceneState}
                                            activeStageNumber={activeStageNumber}
                                            stageCount={pipelineStages.length}
                                            isAnalyzing={isAnalyzing}
                                        />
                                        <div className="scene-overlay">
                                            <div className={`scene-banner scene-banner-${sceneState}`}>
                                                <span className="scene-banner-title">Pipeline Status</span>
                                                <span className="scene-banner-headline">{sceneBannerTitle}</span>
                                                <span className="scene-banner-subtitle">{sceneBannerSubtitle}</span>
                                            </div>
                                        </div>
                                    </div>
                                    <div className="flex items-center justify-between text-[11px] font-bold uppercase tracking-widest text-stone-500">
                                        <span>Sequential preprocessing loop</span>
                                        <span>
                                            {isAnalyzing
                                                ? `Stage ${activeStageNumber} of ${pipelineStages.length}`
                                                : (result ? 'Cycle complete' : 'Idle')}
                                        </span>
                                    </div>
                                </div>

                                <div className="flex flex-col gap-6">
                                    <div className={`status-card status-${sceneState}`}>
                                        <div className="flex items-center gap-3 mb-4">
                                            <div className={`w-11 h-11 rounded-full flex items-center justify-center ${sceneState === 'pass'
                                                ? 'bg-emerald-100 text-emerald-700'
                                                : sceneState === 'fail'
                                                    ? 'bg-red-100 text-red-700'
                                                    : sceneState === 'processing'
                                                        ? 'bg-amber-100 text-amber-700'
                                                        : 'bg-stone-100 text-stone-500'
                                                }`}>
                                                <StatusIcon className="w-6 h-6" />
                                            </div>
                                            <div>
                                                <p className="text-[10px] uppercase tracking-widest text-stone-500 font-bold">Soma Status</p>
                                                <h3 className="text-2xl font-black text-stone-900">{statusHeadline}</h3>
                                            </div>
                                        </div>
                                        <p className="text-sm font-medium text-stone-600 mb-4">{statusMessage}</p>
                                        <div className="rounded-2xl border border-stone-200/60 bg-white/70 px-4 py-3 text-sm font-semibold text-stone-700 flex items-center justify-between">
                                            <span>Current Stage</span>
                                            <span className="text-stone-900">{currentStageLabel}</span>
                                        </div>

                                        {isAnalyzing && !result && (
                                            <div className="mt-4 flex items-center gap-3 text-xs uppercase tracking-widest font-bold text-stone-400">
                                                <div className="w-8 h-2 rounded-full bg-stone-200 overflow-hidden">
                                                    <div className="w-3 h-full bg-stone-500 rounded-full animate-[slideRight_1.5s_ease-in-out_infinite]" />
                                                </div>
                                                <span>Data particles flowing</span>
                                            </div>
                                        )}

                                        {result && (
                                            <div className="mt-4 grid gap-3">
                                                <div className="rounded-2xl border border-white/60 bg-white/70 px-4 py-3 flex items-center justify-between shadow-sm">
                                                    <span className="text-[10px] uppercase tracking-widest text-stone-500 font-bold">Classification</span>
                                                    <span className={`text-sm font-black ${result.is_drone ? 'text-red-700' : 'text-emerald-700'}`
